## Agent skills

### Issue tracker

Issues and specs live under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

The five default triage labels are used. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: use root `CONTEXT.md` and `docs/adr/`. See `docs/agents/domain.md`.


### Rule of Testing

Not every time you write code you need to test it just write and i will check if it run or not