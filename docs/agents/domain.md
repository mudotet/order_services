# Domain Docs

This is a single-context repo. Before domain work, read root `CONTEXT.md` and relevant `docs/adr/` files when they exist. Proceed silently when they do not. Use the glossary’s defined terms and surface any ADR conflict.
